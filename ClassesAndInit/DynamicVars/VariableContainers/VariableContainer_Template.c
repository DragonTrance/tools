//use ctrl+f and replace every instance of "classname" with the class name of your choice
//use ctrl+f and replace every instance of "classtype" with the type of your choice
//remember to search for "changeme" for everything else
//Remove the #if macros in this file that prevents it from being compiled

//Remember to copy the respective header file

#if 0
#include "VariableContainer_classname.h"
VC_DECLARE_SetValue(classname, classtype)
VC_DECLARE_GetValue(classname, classtype)
VC_DECLARE_WriteStream(classname, VC_VALP_CLASSNAME)
VC_DECLARE_Randomize(classname, <class_type>, VC_RAND_CLASSNAME_MIN, VC_RAND_CLASSNAME_MAX)
#endif
